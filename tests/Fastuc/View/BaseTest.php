<?hh 
namespace Fastuc\View;
/*
class BaseTest extends \PHPUnit_Framework_TestCase
{
	private $view;
	
	public function setUp()
	{
		$this->view = new Base();
	}
	
	public function testTemplates()
	{
		$this->assertNull( $this->view->getTemplate() );
	}
	
	public function testVars()
	{
		$this->assertFalse( $this->view->hasParam( 'title' ) );
		
		$this->view->setParam( 'title', 'New page' );
		$this->view->setParam( 'description', 'This is a new page' );
		
		$this->assertTrue( $this->view->hasParam( 'title' ) );
		
		$this->assertEquals( 2, count( $this->view->getParams() ) );
		
		$this->view->setParams( array( 'foo' => 'bar' ) );
		
		$this->assertEquals( 1, count( $this->view->getParams() ) );
		
		$this->view->addParams( array( 'scott' => 'tigger' ) );
		$this->view->addParams( array( 'scott' => 'tigger' ) );
		
		$this->assertEquals( 2, count( $this->view->getParams() ) );
	}
}
 */
